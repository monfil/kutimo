<section class="hero is-light is-large">
  <div class="hero-head">
    <nav class="navbar is-transparent">
      <div class="navbar-brand">
        <%= link_to image_tag("Logo47.png", size: "150x50", alt: "kutimo from the family for the family"), '/' %><!-- logo item (open dashboard if in session, open this page if no session)-->
        <div class="navbar-burger" data-target="NavMenu">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </div>
      <div align="right" id="NavMenu" class="navbar-menu is-boxed">
        <div class="navbar-start">
          <p class="navbar-item"> <!-- Menu Login link -->
            <% if logged_in? %>
              <strong>
                <%= link_to "Logout", logout_path, method: :delete %>
              </strong>
            <% else %>
              <strong>
                <%= link_to "Login", login_path %>
              </strong>
            <% end %>
          </p>
        </div>  
      </div>
    </nav>  
  </div>
</section>